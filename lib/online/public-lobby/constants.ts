/** Public lobby visible in browse list after publish (TZ §6.4). */
export const PUBLIC_LOBBY_TTL_MS = 5 * 60 * 1000;

/** Max players in a public room. */
export const PUBLIC_LOBBY_MAX_PLAYERS = 8;

/** Browse list page size (TZ §6.4.1). */
export const PUBLIC_LOBBY_PAGE_SIZE = 20;

name: Legal pages (GitHub Pages)

on:
  push:
    branches: [main, dev]
    paths:
      - 'docs/legal/**'
      - 'docs/wordreapers_about.md'
      - 'assets/icons/app-icon-512x512.png'
      - 'assets/store-badges/**'
      - 'scripts/build-legal-pages.ts'
      - '.github/workflows/legal-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: legal-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Use npm 11
        run: npm install -g npm@11.6.2

      - name: Install dependencies
        run: npm ci

      - name: Build legal HTML pages
        run: npm run legal:pages

      - name: Upload pages artifact
        uses: actions/upload-artifact@v5
        with:
          name: legal-pages
          path: assets/generated/legal-pages
          if-no-files-found: error

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Download pages artifact
        uses: actions/download-artifact@v5
        with:
          name: legal-pages
          path: assets/generated/legal-pages

      # Requires repo Settings → Pages → Build and deployment → Source: GitHub Actions.
      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: assets/generated/legal-pages

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
